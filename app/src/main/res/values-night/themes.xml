<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for night mode (dark theme). -->
    <style name="Base.Theme.ProfilePageShopee" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowBackground">@color/black</item>
        <item name="colorPrimary">@color/primary</item> <!-- Black primary color -->
        <item name="colorSecondary">@color/secondary</item> <!-- Dark gray secondary color -->
    </style>

    <style name="Theme.ProfilePage" parent="Base.Theme.ProfilePageShopee" />

</resources>
